# Day 3 - CLI To-Do App

A simple command-line To-Do application built with Python.

## Features

- Add tasks
- List tasks
- Remove tasks by number
- Persist tasks in `tasks.json`
- Handle invalid task numbers with a friendly message

## Setup

Create and activate a virtual environment:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1